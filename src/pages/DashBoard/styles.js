import styled from 'styled-components';

export default styled.div`
	display: flex;
	overflow: auto;

	main {
		display: flex;
		padding: 5.37rem 10.7rem;
		flex-wrap: wrap;
		overflow: auto;
		flex: 1;
	}
`;
